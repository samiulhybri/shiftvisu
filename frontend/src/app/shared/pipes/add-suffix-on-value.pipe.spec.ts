import { AddSuffixOnValuePipe } from './add-suffix-on-value.pipe';

describe('AddSuffixOnValuePipe', () => {
  it('create an instance', () => {
    const pipe = new AddSuffixOnValuePipe();
    expect(pipe).toBeTruthy();
  });
});
