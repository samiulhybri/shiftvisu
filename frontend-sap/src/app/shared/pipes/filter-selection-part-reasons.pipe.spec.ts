import { FilterSelectionPartReasonsPipe } from './filter-selection-part-reasons.pipe';

describe('FilterSelectionPartReasonsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSelectionPartReasonsPipe();
    expect(pipe).toBeTruthy();
  });
});
